--- File: /home/sk/Desktop/Slot_Booking Application/src/main.jsx ---

import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)


--- File: /home/sk/Desktop/Slot_Booking Application/src/App.jsx ---

import React from "react";
import { BrowserRouter, Route, Routes } from "react-router-dom";
import Home from "./pages/Home";
import BookingCalendar from "./pages/BookingCalendar";
import Profile from "./pages/Profile";
import Header from "./components/Header";
import Footer from "./components/Footer";

const App = () => {
  return (
    <BrowserRouter>
      <Header />
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/booking" element={<BookingCalendar />} />
        <Route path="/profile" element={<Profile />} />
        {/* Fallback route can be added here */}
      </Routes>
      <Footer />
    </BrowserRouter>
  );
};

export default App;


--- File: /home/sk/Desktop/Slot_Booking Application/src/index.css ---

@tailwind base;
@tailwind components;
@tailwind utilities;



--- File: /home/sk/Desktop/Slot_Booking Application/src/pages/BookingCalendar.jsx ---

import React, { useState } from 'react';

const BookingCalendar = () => {
  const [isDark, setIsDark] = useState(false);
  const username = "JohnDoe"; // Placeholder username

  // State for slot management
  const [availabilities, setAvailabilities] = useState([]);
  const [newDate, setNewDate] = useState("");
  const [newStartTime, setNewStartTime] = useState("");
  const [newEndTime, setNewEndTime] = useState("");
  const [timezone, setTimezone] = useState(Intl.DateTimeFormat().resolvedOptions().timeZone);
  const [copySourceDate, setCopySourceDate] = useState("");
  const [copyTargetDate, setCopyTargetDate] = useState("");

  const handleAddAvailability = (e) => {
    e.preventDefault();
    const newSlot = {
      id: Date.now(),
      date: newDate,
      startTime: newStartTime,
      endTime: newEndTime
    };
    setAvailabilities([...availabilities, newSlot]);
    setNewDate("");
    setNewStartTime("");
    setNewEndTime("");
  };

  const handleDeleteAvailability = (id) => {
    setAvailabilities(availabilities.filter(slot => slot.id !== id));
  };

  const handleCopyAvailability = () => {
    if(copySourceDate && copyTargetDate) {
      const slotsToCopy = availabilities.filter(slot => slot.date === copySourceDate);
      const copiedSlots = slotsToCopy.map(slot => ({
        ...slot,
        id: Date.now() + Math.random(), // ensure unique id
        date: copyTargetDate,
      }));
      setAvailabilities([...availabilities, ...copiedSlots]);
    }
  };

  return (
    <div className={isDark ? "dark" : ""}>
      <div className="min-h-screen bg-white dark:bg-gray-900 transition-colors duration-500">
        {/* Header with username and dark mode toggle */}
        <header className="py-4 bg-gray-200 dark:bg-gray-800 flex justify-between items-center px-4">
          <h1 className="text-xl font-bold text-gray-900 dark:text-gray-100">
            Slot Booking Dashboard - {username}
          </h1>
          <button
            onClick={() => setIsDark(!isDark)}
            className="px-3 py-2 bg-black text-white rounded transition-colors duration-300"
          >
            Toggle {isDark ? "Light" : "Dark"} Mode
          </button>
        </header>

        <main className="px-4 py-6">
          {/* Calendar & Upcoming Events Section */}
          <section className="relative bg-stone-50 transition-all duration-300 rounded-xl overflow-hidden">
            {/* Background animated elements */}
            <div className="bg-sky-400 w-full sm:w-40 h-40 rounded-full absolute top-1 opacity-20 max-sm:right-0 sm:left-56 z-0"></div>
            <div className="bg-emerald-500 w-full sm:w-40 h-24 absolute top-0 -left-0 opacity-20 z-0"></div>
            <div className="bg-purple-600 w-full sm:w-40 h-24 absolute top-40 -left-0 opacity-20 z-0"></div>
            <div className="w-full py-24 relative z-10 backdrop-blur-3xl">
              <div className="w-full max-w-7xl mx-auto px-2 lg:px-8">
                <div className="grid grid-cols-12 gap-8 max-w-4xl mx-auto xl:max-w-full">
                  {/* Left Panel: Upcoming Events */}
                  <div className="col-span-12 xl:col-span-5">
                    <h2 className="font-manrope text-3xl leading-tight text-gray-900 mb-1.5">
                      Upcoming Events
                    </h2>
                    <p className="text-lg font-normal text-gray-600 mb-8">
                      Don’t miss a schedule!
                    </p>
                    <div className="flex gap-5 flex-col">
                      {/* Sample events */}
                      <div className="p-6 rounded-xl bg-white shadow transition-all duration-300 hover:shadow-lg">
                        <div className="flex items-center justify-between mb-3">
                          <div className="flex items-center gap-2.5">
                            <span className="w-2.5 h-2.5 rounded-full bg-purple-600"></span>
                            <p className="text-base font-medium text-gray-900">
                              Jan 10, 2020 - 10:00 - 11:00
                            </p>
                          </div>
                          <div className="dropdown relative inline-flex">
                            <button type="button" className="inline-flex justify-center py-2.5 px-1 items-center gap-2 text-sm text-black rounded-full cursor-pointer font-semibold shadow transition-all duration-500 hover:text-purple-600">
                              <svg xmlns="http://www.w3.org/2000/svg" width="12" height="4" viewBox="0 0 12 4" fill="none">
                                <path d="M1.85624 2.00085H1.81458M6.0343 2.00085H5.99263M10.2124 2.00085H10.1707" stroke="currentcolor" strokeWidth="2.5" strokeLinecap="round"></path>
                              </svg>
                            </button>
                          </div>
                        </div>
                        <h6 className="text-xl leading-8 font-semibold text-black mb-1">Meeting with a Friend</h6>
                        <p className="text-base font-normal text-gray-600">Travel discussion</p>
                      </div>
                      {/* Additional events can be added here */}
                    </div>
                  </div>
                  {/* Right Panel: Calendar */}
                  <div className="col-span-12 xl:col-span-7 px-2.5 py-5 sm:p-8 bg-gradient-to-b from-white/25 to-white xl:bg-white rounded-2xl max-xl:row-start-1 shadow transition-all duration-500">
                    <div className="flex flex-col md:flex-row gap-4 items-center justify-between mb-5">
                      <div className="flex items-center gap-4">
                        <h5 className="text-xl leading-8 font-semibold text-gray-900">January 2024</h5>
                        <div className="flex items-center">
                          <button className="text-black p-1 rounded transition-all duration-300 hover:text-white hover:bg-black">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none">
                              <path d="M10.0002 11.9999L6 7.99971L10.0025 3.99719" stroke="currentcolor" strokeWidth="1.3" strokeLinecap="round" strokeLinejoin="round"></path>
                            </svg>
                          </button>
                          <button className="text-black p-1 rounded transition-all duration-300 hover:text-white hover:bg-black">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none">
                              <path d="M6.00236 3.99707L10.0025 7.99723L6 11.9998" stroke="currentcolor" strokeWidth="1.3" strokeLinecap="round" strokeLinejoin="round"></path>
                            </svg>
                          </button>
                        </div>
                      </div>
                      <div className="flex items-center rounded-md p-1 bg-indigo-50 gap-px">
                        <button className="py-2.5 px-5 rounded-lg bg-indigo-50 text-black text-sm font-medium transition-all duration-300 hover:bg-black hover:text-white">
                          Day
                        </button>
                        <button className="py-2.5 px-5 rounded-lg bg-black text-white text-sm font-medium transition-all duration-300 hover:bg-black hover:text-white">
                          Week
                        </button>
                        <button className="py-2.5 px-5 rounded-lg bg-indigo-50 text-black text-sm font-medium transition-all duration-300 hover:bg-black hover:text-white">
                          Month
                        </button>
                      </div>
                    </div>
                    <div className="border border-indigo-200 rounded-xl">
                      <div className="grid grid-cols-7 rounded-t-3xl border-b border-indigo-200">
                        {["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map(day => (
                          <div key={day} className="py-3.5 border-r last:border-r-0 bg-indigo-50 flex items-center justify-center text-sm font-medium text-black">
                            {day}
                          </div>
                        ))}
                      </div>
                      <div className="grid grid-cols-7 rounded-b-xl">
                        {/* Calendar cells (for brevity only a few sample cells are shown) */}
                        {Array.from({ length: 35 }, (_, i) => (
                          <div key={i} className={`flex xl:aspect-square max-xl:min-h-[60px] p-3.5 border ${i < 5 ? 'bg-gray-50' : 'bg-white'} border-indigo-200 transition-all duration-300 hover:bg-indigo-50 cursor-pointer`}>
                            <span className={`text-xs font-semibold ${i < 5 ? 'text-gray-400' : 'text-gray-900'}`}>{i + 1}</span>
                          </div>
                        ))}
                      </div>
                    </div>
                  </div>
                  {/* End Right Panel */}
                </div>
              </div>
            </div>
          </section>

          {/* New: Manage Availability Section */}
          <section className="p-4 mt-8 bg-gray-100 dark:bg-gray-800 rounded shadow transition-all duration-500">
            <h2 className="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-4">Manage Availability</h2>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              {/* Add New Availability Form */}
              <div>
                <h3 className="text-xl text-gray-800 dark:text-gray-200 mb-2">Add New Availability</h3>
                <form onSubmit={handleAddAvailability} className="space-y-4">
                  <div>
                    <label className="block text-gray-700 dark:text-gray-300">Date</label>
                    <input
                      type="date"
                      value={newDate}
                      onChange={(e) => setNewDate(e.target.value)}
                      className="mt-1 p-2 w-full rounded border"
                      required
                    />
                  </div>
                  <div>
                    <label className="block text-gray-700 dark:text-gray-300">Start Time</label>
                    <input
                      type="time"
                      value={newStartTime}
                      onChange={(e) => setNewStartTime(e.target.value)}
                      className="mt-1 p-2 w-full rounded border"
                      required
                    />
                  </div>
                  <div>
                    <label className="block text-gray-700 dark:text-gray-300">End Time</label>
                    <input
                      type="time"
                      value={newEndTime}
                      onChange={(e) => setNewEndTime(e.target.value)}
                      className="mt-1 p-2 w-full rounded border"
                      required
                    />
                  </div>
                  <button type="submit" className="px-4 py-2 bg-black text-white rounded transition-all duration-300 hover:bg-indigo-700">
                    Add Availability
                  </button>
                </form>
              </div>
              {/* List of Added Availabilities */}
              <div>
                <h3 className="text-xl text-gray-800 dark:text-gray-200 mb-2">Your Availabilities</h3>
                <ul>
                  {availabilities.length === 0 ? (
                    <li className="text-gray-600 dark:text-gray-300">No availabilities added yet.</li>
                  ) : (
                    availabilities.map((slot) => (
                      <li
                        key={slot.id}
                        className="flex justify-between items-center p-2 bg-white dark:bg-gray-700 rounded mb-2 transition-all duration-300"
                      >
                        <div>
                          <p className="text-gray-900 dark:text-gray-100">
                            {slot.date}: {slot.startTime} - {slot.endTime}
                          </p>
                        </div>
                        <button
                          onClick={() => handleDeleteAvailability(slot.id)}
                          className="px-2 py-1 bg-red-500 text-white rounded hover:bg-red-600 transition-all duration-300"
                        >
                          Delete
                        </button>
                      </li>
                    ))
                  )}
                </ul>
              </div>
            </div>

            {/* Timezone and Copy Availability */}
            <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <h3 className="text-xl text-gray-800 dark:text-gray-200 mb-2">Timezone</h3>
                <select
                  value={timezone}
                  onChange={(e) => setTimezone(e.target.value)}
                  className="p-2 rounded border w-full"
                >
                  <option value="America/New_York">America/New_York</option>
                  <option value="Europe/London">Europe/London</option>
                  <option value="Asia/Kolkata">Asia/Kolkata</option>
                  <option value="Europe/Berlin">Europe/Berlin</option>
                  {/* Additional timezones can be added */}
                </select>
              </div>
              <div>
                <h3 className="text-xl text-gray-800 dark:text-gray-200 mb-2">Copy Availability</h3>
                <div className="space-y-4">
                  <div>
                    <label className="block text-gray-700 dark:text-gray-300">From Date</label>
                    <input
                      type="date"
                      value={copySourceDate}
                      onChange={(e) => setCopySourceDate(e.target.value)}
                      className="mt-1 p-2 w-full rounded border"
                    />
                  </div>
                  <div>
                    <label className="block text-gray-700 dark:text-gray-300">To Date</label>
                    <input
                      type="date"
                      value={copyTargetDate}
                      onChange={(e) => setCopyTargetDate(e.target.value)}
                      className="mt-1 p-2 w-full rounded border"
                    />
                  </div>
                  <button
                    onClick={handleCopyAvailability}
                    className="px-4 py-2 bg-green-600 text-white rounded transition-all duration-300 hover:bg-green-700"
                  >
                    Copy Availability
                  </button>
                </div>
              </div>
            </div>
          </section>
        </main>
      </div>
    </div>
  );
};

export default BookingCalendar;


--- File: /home/sk/Desktop/Slot_Booking Application/src/pages/Home.jsx ---

import React from 'react';

function Home() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100 dark:bg-gray-900 transition-colors duration-500">
      <h1 className="text-4xl font-bold text-gray-900 dark:text-gray-100">
        Welcome to the Slot Booking App!
      </h1>
    </div>
  );
}

export default Home;


--- File: /home/sk/Desktop/Slot_Booking Application/src/pages/Profile.jsx ---

import React from 'react';

const Profile = () => {
  const username = "JohnDoe"; // Placeholder
  const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone;
  
  return (
    <div className="min-h-screen bg-gray-100 dark:bg-gray-900 p-6 transition-colors duration-500">
      <h1 className="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-4">User Profile</h1>
      <div className="bg-white dark:bg-gray-800 p-4 rounded shadow">
        <p className="text-lg text-gray-800 dark:text-gray-200">Username: {username}</p>
        <p className="text-lg text-gray-800 dark:text-gray-200">Timezone: {timezone}</p>
        {/* Additional user details can be added here */}
      </div>
    </div>
  );
};

export default Profile;


--- File: /home/sk/Desktop/Slot_Booking Application/src/components/Header.jsx ---

import React from 'react';
import { Link } from 'react-router-dom';

function Header() {
  return (
    <header className="bg-gray-800 text-white p-4 flex justify-between items-center">
      <div className="text-xl font-bold">Slot Booking App</div>
      <nav>
        <ul className="flex space-x-4">
          <li>
            <Link to="/" className="hover:text-indigo-300 transition-colors duration-300">Home</Link>
          </li>
          <li>
            <Link to="/booking" className="hover:text-indigo-300 transition-colors duration-300">Dashboard</Link>
          </li>
          <li>
            <Link to="/profile" className="hover:text-indigo-300 transition-colors duration-300">Profile</Link>
          </li>
        </ul>
      </nav>
    </header>
  );
}

export default Header;


--- File: /home/sk/Desktop/Slot_Booking Application/src/components/Footer.jsx ---

import React from 'react';

function Footer() {
  return (
    <footer className="bg-gray-200 dark:bg-gray-800 p-4 text-center text-gray-700 dark:text-gray-300">
      &copy; {new Date().getFullYear()} Slot Booking App. All rights reserved.
    </footer>
  );
}

export default Footer;


